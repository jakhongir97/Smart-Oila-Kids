# macOS
.DS_Store

# Xcode user data and build output
DerivedData/
*.xcuserstate
*.xcscmblueprint
**/xcuserdata/
**/project.xcworkspace/xcuserdata/

# SwiftPM / local build artifacts
.build/
Artifacts/

# Legacy archives / imported snapshots
LegacyArchive_20260224/
__MACOSX/
tmp/

# Python caches (script tests)
__pycache__/
*.pyc

# xcodebuild default output dir (extension standalone builds)
build/

# Firebase config — holds the project API key. This repo is PUBLIC; keep the plist out of it.
# Obtain from the Firebase console (project oila360, iOS app uz.smartoila.kids) and drop into
# SmartOilaKids/Resources/. Already registered in project.pbxproj, so no Xcode step is needed.
SmartOilaKids/Resources/GoogleService-Info.plist
